Example docker/node Dockerfile used to generate Docker images for node.js
based applications.

to build an image run ```docker build -t <my-tag> .```

Based on the official ```docker/node:buster-lts Dockerfile``` examples
